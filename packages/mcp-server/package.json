{
  "name": "@missionthread/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local, read-only MCP server exposing MissionThread AI program data over stdio. Built in Phase 7.",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "build": "node build.mjs",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@missionthread/core": "*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "dotenv": "^16.6.1",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
