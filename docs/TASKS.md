# Tasks

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done

This file must stay detailed enough for a new session to resume without chat history.

## Phase 0 — Plan

- [x] Place `PROJECT_GUIDE.md`, `docs/SPEC.md`
- [x] Inspect repository (empty except README; Node v25.2.1 was the local default — Node 25 is EOL, not LTS; nvm, docker, docker compose, local Postgres@5432 all present)
- [x] Propose architecture, Prisma model (20 models, 3 merges proposed), risks, Node 24.x LTS, Docker Compose port 55432, Phase 1 commands
- [x] Write `docs/IMPLEMENTATION_PLAN.md`, `docs/TASKS.md`, `docs/DECISIONS.md`, `docs/ARCHITECTURE.md`
- [x] User approved the Phase 0 plan
- [x] User said `continue` to authorize Phase 1

## Phase 1 — Foundation — done

- [x] Node 24.18.0 installed via nvm; `.nvmrc` pinned to the exact patch
- [x] Root `package.json` with `workspaces: ["apps/*", "packages/*"]`, `engines.node: ">=24 <25"`
- [x] `apps/web`: `create-next-app` (TypeScript, Tailwind, ESLint, App Router, `src/`, `@/*` alias)
- [x] `packages/core`: `src/`, `prisma/`, deps `zod`, `@prisma/client`, `@prisma/adapter-pg`; devDeps `prisma`, `typescript`, `vitest`, `tsx`, `dotenv`
- [x] `packages/mcp-server`: placeholder scaffold only, no logic yet
- [x] Prisma schema at `packages/core/prisma/schema.prisma` — 20-model set from Phase 0, all 3 approved merges applied (`TestResult`→`TestCase`, `SupplierUpdate`→`ProgramEvent`, `Approval`→`Decision`)
- [x] `docker-compose.yml`: Postgres service, host port `55432`, `missionthread_dev` + `missionthread_test` logical databases (via `docker/init-test-db.sh`)
- [x] `.env.example`, `.env.test.example` at repo root reflecting the 55432 port and `DATABASE_URL` / `TEST_DATABASE_URL`; `apps/web/.env` is a symlink to the root `.env` (Next.js only reads env files from its own directory)
- [x] Migration + deterministic seed script using fixed seed IDs from `SPEC.md` §4, exact seed counts (8 requirements, 6 components, 8 milestones, 8 dependency edges, 8 tests mixed outcomes, 4 risks, 5 budget items, 3 suppliers, 3 defects, 4 events, 3 users) — verified in both `missionthread_dev` and `missionthread_test`
- [x] Auth.js v5 (`next-auth@5.0.0-beta.31`) Credentials provider, JWT session strategy explicit, Zod-validated input, `crypto.scrypt` + `crypto.timingSafeEqual` password verification, no Account/Session/VerificationToken models, no middleware/proxy (auth checked via `auth()` in server layouts/pages)
- [x] 3 seeded demo users, one per role — login flow verified end-to-end via real HTTP requests (correct credentials succeed, wrong password rejected, session role surfaced correctly for all 3 roles)
- [x] Base layout / nav shell — clean/flat Tailwind design, `/`, `/programs/edgelink-x`, `/audit` all render behind auth
- [x] Test-reset script (`packages/core/scripts/reset-test-db.ts`) refuses to run unless target DB name contains "test" — verified it refuses against `missionthread_dev` and succeeds against `missionthread_test`.
      **Superseded by the 2026-07-18 correction pass** (substring→token-boundary+allowlist) **and the 2026-07-18 second correction pass** (allowlist→exact approved `(host, port, database)` target tuples; a name merely containing "test" on an unapproved port/host no longer passes). See "Phase 1 second correction pass" below.
- [x] `.github/workflows/ci.yml`: install with lockfile, `node-version-file: .nvmrc`, `prisma validate` + `generate`, lint, format check, type check, unit tests, production build; `AI_MODE=mock` always.
      **Superseded by the correction passes:** CI now also includes a real `postgres:17-alpine` service, `permissions: contents: read`, migration + deterministic seed against that service database, a production build, and an application smoke test — see "Phase 1 correction pass" and "Phase 1 second correction pass" below.
- [x] `.dockerignore`, `Dockerfile` stub (not build-tested; full Docker build verification is Phase 8).
      **Superseded by the 2026-07-18 correction pass:** standalone Docker image built, started, and `/login` verified live (see below) — Docker support exists now, not deferred to Phase 8.
- [x] Ran full Phase 1 quality gate locally — see Phase 1 report in conversation / commit history for details; all checks passed
- [x] Updated this file and `docs/DECISIONS.md`

### Known Phase 1 blockers/risks carried forward

- `next-auth` pinned to the v5 **beta** channel (`5.0.0-beta.31`) — the version Auth.js's own current docs recommend for the App Router, but pre-1.0.
- Moderate `npm audit` advisories in transitive dev-tooling dependencies (nested `@prisma/dev` → old `@hono/node-server`; Next's bundled `postcss` copy). Suggested auto-fixes downgrade Prisma/Next to breaking versions — not applied. Revisit when upstream ships non-breaking patches.
- A real deployment needs `AUTH_TRUST_HOST=true` or an explicit `AUTH_URL` set at runtime (Auth.js v5 rejects untrusted `Host` headers by default) — not needed for local dev. Now documented with a runtime example in README.md "Docker" (2026-07-18 second correction pass).

## Phase 1 correction pass — done (2026-07-18)

Independent review requested against the Phase 1 implementation; findings were verified against the actual repository (not accepted blindly) before fixing. Full disposition and verification detail is in `docs/DECISIONS.md`'s 2026-07-18 "(correction pass)" entries and this repository's commit history. Summary of what changed:

- [x] **Critical:** fixed a malformed-hash authentication bypass in `verifyPassword` (empty-buffer `timingSafeEqual` true-positive) — reproduced, fixed with full field validation, switched to async `crypto.scrypt`, 22 new regression tests.
- [x] Replaced substring-based test-database-name matching with a token-boundary rule + allowlist; built one shared `checkDestructiveOperationAllowed()` guard (production check, host allowlist, database-name allowlist, explicit `ALLOW_DESTRUCTIVE_DATABASE_OPERATION=true` opt-in, sanitized error messages) now used by both `reset-test-db.ts` and `seed.ts`'s previously-unguarded clear step; positive + negative unit tests.
- [x] Deterministic IDs for `User` (`USER-PM`/`USER-ENG-LEAD`/`USER-EXEC`) and `Dependency` (`DEP-001`..`DEP-008`); each demo user now gets its own `hashPassword()` call (was one shared hash/salt for all three).
- [x] `RecordType` expanded (added `DEPENDENCY` + workflow-entity kinds) plus three Zod context allowlists (`evidenceRecordTypeSchema`, `proposedChangeTargetTypeSchema`, `auditTargetTypeSchema`); `SourceReference` uniqueness constraint; `Decision.traceId` index. Migration `20260718055852_expand_record_type_and_add_constraints`, applied to both databases.
- [x] Documented the `ImpactAnalysis` one-row-per-attempt lifecycle decision (no schema change).
- [x] Repaired the Dockerfile (Next standalone output, non-secret build-time `DATABASE_URL`, no root `package.json` needed at runtime) — build, container start, and `GET /login` all verified live; container and test image cleaned up afterward.
- [x] Neutralized remaining tool-identifying wording (`docs/DECISIONS.md` x2, `docs/assets/README.md`); removed the tracked `.gitignore` entry for the tool-specific local settings directory in favor of a local, unshared `.git/info/exclude` entry.
- [x] Wired up the real banner image (1280×640, now present at `docs/assets/missionthread-ai-banner.png`); removed the stale placeholder note; replaced `apps/web/README.md` boilerplate.
- [x] Corrected Node-25-EOL wording in `docs/TASKS.md`/`docs/DECISIONS.md`; marked `docs/ARCHITECTURE.md` sections as implemented vs. planned; changed `package.json` description from "agentic" to "AI-assisted".
- [x] Expanded `packages/core`'s lint script to cover `prisma/seed.ts`, `scripts/`, `prisma.config.ts`, `vitest.config.ts` (previously only `src/`).
- [x] Consolidated `/`, `/audit`, `/programs/edgelink-x` under one `(app)` route group + shared layout (`requireSession()`/`Nav` called once, not 3×); added active-link nav state and a horizontal-scroll mobile nav treatment.
- [x] Added an automated 21-check smoke test (`apps/web/scripts/smoke-test.mjs`, `npm run smoke:test`) covering auth, session contents, dashboard data, and sign-out against the dedicated test database; hardened CI with a real `postgres:17-alpine` service, `permissions: contents: read`, a non-secret CI `AUTH_SECRET`, migrate+seed steps, and a smoke-test step.
- [x] Re-ran the full quality gate after all changes: lint, format check, typecheck (3 workspaces), 60 unit tests, production build, and the smoke test (21/21) all pass.

## Phase 1 second correction pass — done (2026-07-18)

A second independent review, verified against the actual repository before fixing (not accepted blindly). Full disposition and verification detail is in `docs/DECISIONS.md`'s "(second correction pass)" entries and this repository's commit history. Summary of what changed:

- [x] Replaced independent host/database-name allowlists with exact `(host, port, database)` target tuples (`ApprovedDatabaseTarget`) — closes a real gap where the port was never validated (`localhost:5432/missionthread_dev`, the wrong port, would previously have passed). Added a CI-only target tuple that only matches when `CI=true`. Dropped a dead `"postgres"` (Compose-internal hostname) allowlist entry — no application container currently resolves it. Intentionally omitted IPv6 support (verified Node's `URL.hostname` bracket-wraps `"::1"` as `"[::1]"`, which the previous unbracketed allowlist entry could never have matched anyway). 39 new/updated unit tests, all passing.
- [x] Removed `ALLOW_DESTRUCTIVE_DATABASE_OPERATION=true` from `.env.example`/`.env.test.example` (previously shipped as permanently enabled). Destructive commands renamed for obvious intent: `packages/core` scripts are now `db:seed:internal`/`db:reset:test:internal`; root scripts `db:seed:destructive` and `db:reset:test` supply the opt-in flag only for their own child process via a new cross-platform `scripts/with-destructive-auth.mjs` wrapper. CI's seed step now sets the flag only on that one step, not workflow-wide.
- [x] Replaced the `docs/DECISIONS.md` entry naming the third-party destructive-operation safety mechanism and quoting the authorization exchange with a neutral, permanent "Destructive-operation authorization policy" (fresh authorization required per invocation, never reused).
- [x] Fixed remaining Node-25 wording in README.md (was "Current-only") to the exact required sentence; audited `docs/IMPLEMENTATION_PLAN.md`/`docs/ARCHITECTURE.md` — neither mentioned Node 25.
- [x] Annotated this file's stale Phase 1 entries (test-reset description, CI description, Dockerfile description) as superseded rather than silently rewriting them — see above.
- [x] README.md: split database commands into safe/non-destructive vs. destructive with the new command names; documented that seeding clears existing data; added `npm run smoke:test` to the quality gate section; added a full "Docker" section (`docker build`, a runtime example using `host.docker.internal` since a container can't reach the host's Postgres via its own `localhost`, required runtime variables, `AUTH_TRUST_HOST`). Fixed the Dockerfile's comment claiming `docker-compose.yml` defines application runtime config (it only defines the Postgres service).
- [x] Hardened `apps/web/scripts/smoke-test.mjs`: added `checkRedirectToLogin()`, which verifies both the redirect status code and that `Location` actually resolves to `/login` with no query string (previously only the status code was checked); replaced the brittle `dashboardHtml.includes(">8<")` requirement-count assertion with `data-testid="stat-value-requirementCount"`/`stat-label-requirementCount` attributes on the dashboard page and a `getTestIdText()` helper that reads the specific labeled value.
- [x] Documented two deferred Phase 4/5 schema decisions without implementing them: `ProposedChangeType.NEW_ACTION` conflicts with `ProposedChange`'s non-nullable target fields (planned fix: nullable fields + Zod discriminated union, Phase 5); `ImpactAnalysis` has no stable grouping key for retry-pair attempts (planned fix: `analysisRunId` + `(analysisRunId, attempt)` uniqueness, Phase 4). Added matching inline schema comments. No schema/migration change made — SPEC.md doesn't require either before Phase 2.
- [x] Added the standing contextual-comment rule to `PROJECT_GUIDE.md` working rules.
- [x] Re-ran the full quality gate after all changes, plus a fresh Docker build/start/`/login` verification cycle — all passing.

## Phase 1 third correction pass — done (2026-07-18)

A third independent review, verified against the actual repository before fixing (not accepted blindly). Full disposition and verification detail is in `docs/DECISIONS.md`'s "(third correction pass)" entries and this repository's commit history. Summary of what changed:

- [x] The GitHub Actions target tuple now requires `GITHUB_ACTIONS=true` specifically, not the generic `CI=true` (which other CI providers and local shells can also set). Renamed `requiresCi`→`requiresGitHubActions` and `CI_TEST_TARGETS`→`GITHUB_ACTIONS_TEST_TARGETS` throughout. 16 new/updated unit tests.
- [x] Destructive seed commands are now target-specific instead of one command authorizing whichever target `DATABASE_URL` happened to point at. `seed.ts` requires an explicit `MISSIONTHREAD_SEED_SCOPE` (`dev`/`test`/`github-actions`, resolved via the new `resolveSeedScopeTargets()`), set only for the seed child process by an extended `scripts/with-destructive-auth.mjs --scope=<value>`. Public, local-development commands: `npm run db:seed:dev:destructive` (dev only) and `npm run db:reset:test` (test only, unchanged name). Separately, `npm run db:seed:github-actions:internal` is an internal, CI-only command — it is never intended to be run locally by a developer, only by `.github/workflows/ci.yml` against the GitHub Actions service-container database. The old unscoped `db:seed:destructive` no longer exists.
- [x] `.github/workflows/ci.yml`'s seed step now calls `db:seed:github-actions:internal` instead of the old generic command; the workflow-level `env:` block still sets no destructive-authorization variables.
- [x] Marked the first correction pass's now-fully-superseded "Shared destructive-database-operation guard" entry in `docs/DECISIONS.md` with a prominent superseded notice pointing to the second and third pass entries, rather than leaving it to be mistaken for current behavior.
- [x] Replaced remaining private-conversation-dependent and tool-identifying wording in this file ("the conversation['s ...] report" ×2, `.claude/`, "AI-agent consent mechanism") with durable, neutral wording pointing at `docs/DECISIONS.md` and commit history.
- [x] README.md: renamed `db:seed:destructive` references to `db:seed:dev:destructive`, documented the three-command split (dev/test/CI) and that scope is never inferred from `DATABASE_URL`, and replaced the hardcoded "21 checks" smoke-test count with non-stale wording pointing at the script itself.
- [x] Re-ran the full quality gate after all changes; re-verified the target-specific destructive commands live against their intended local databases.

## Phase 2 — Deterministic program logic — done (2026-07-18)

All eleven `SPEC.md` §8 functions implemented in `packages/core/src/analysis/`, read-only, no AI dependency, no new Prisma models/migrations. Full disposition and every formula/ambiguity decision is in `docs/DECISIONS.md`'s "(Phase 2)" entries.

- [x] Inspected the actual schema and seed relationships before implementing (Component↔Requirement via `RequirementComponent`; Component→Milestone direct + `Dependency` DAG for cascades; Requirement↔TestCase via `TestRequirement`; Defect→TestCase via `relatedTestCaseId`; Risk/BudgetItem→Component; `ProgramEvent`→Component/Supplier). No ambiguity required a schema change; none was made.
- [x] Module structure: `packages/core/src/analysis/{types,schemas,traceability,dependencies,verification,defects,budget,schedule,risk,readiness,evidence,index}.ts`. Public API re-exported through `packages/core/src/index.ts`.
- [x] One consistent error strategy for the whole layer: every function returns `ServiceResult<T> = { ok: true; data } | { ok: false; error: DomainError }` (`NOT_FOUND` / `VALIDATION_ERROR`); nothing throws for an expected failure; raw Prisma errors never reach a caller.
- [x] Input validation via Zod (`entityIdSchema`, `entityIdArraySchema`): rejects empty, whitespace-only, and padded IDs; rejects duplicate IDs in array inputs; empty arrays are accepted (not malformed).
- [x] `getImpactedRequirements(componentId)` / `getImpactedMilestones(componentId)` — direct links plus (for milestones) dependency-derived cascade impacts, deduplicated, deterministically ordered. 11 tests including a live "existing component, zero links" case (temporary isolated fixture, cleaned up).
- [x] `getDependencyChain(milestoneId)` — `fromMilestoneId` is upstream (prerequisite), `toMilestoneId` is downstream (dependent), returned as separate arrays. Cycle/duplicate-edge protection implemented in a pure, DB-free BFS (`traverseDependencyChain`) unit-tested directly with fabricated cyclic/branching/duplicate-edge data — no cycle was ever seeded into Postgres. One query for every edge/milestone in a program, not one per hop. 13 tests.
- [x] `getVerificationGaps(requirementIds)` — worst-outcome-wins gap classification (FAILED > BLOCKED > NOT_RUN > NONE; zero tests = NO_COVERAGE), missing requirement IDs reported explicitly, not dropped. 10 tests.
- [x] `getRelatedDefects(requirementIds)` — Requirement→TestCase→Defect path only, never text matching; pure grouping step (`groupRelatedDefects`) unit-tested with synthetic multi-requirement-per-defect and duplicate-link data the real seed doesn't naturally exercise. 9 tests.
- [x] `calculateBudgetVariance(programId)` / `calculateBudgetExposure(eventId)` — `Prisma.Decimal` arithmetic throughout (no binary-float drift), `varianceAmount = actual - planned` sign convention, mixed-currency handling (`byCurrency` breakdown, top-level fields `null` rather than a wrong cross-currency sum), zero-planned-amount handled as `null` percentage. Exposure is bounded to budget items linked via the event's component; `totalDeterministicExposure` is documented as "budget at risk," not an invented incremental delay cost (schema has no such field). 13 tests.
- [x] `calculateScheduleExposure(eventId)` — UTC calendar-day arithmetic (`utcDayDifference`/`addUtcDays`, pure and unit-tested independent of the DB: 28-day seed case, same-day, reversed, month boundary, leap year); detects and reports (never silently accepts) a stored `delayDays` that disagrees with the two dates; `latestExposedDate` = max(impacted milestone planned date + direct delay). 13 tests.
- [x] `calculateRiskScore(riskId)` — `probability × impact`, documented band mapping (1–4 LOW / 5–9 MEDIUM / 10–14 HIGH / 15–25 CRITICAL), never lets a stored severity label silently override the numeric result. Surfaced a genuine pre-existing seed inconsistency: `RISK-003` computes LOW but is stored as MEDIUM — kept as-is and regression-tested, not "fixed" by changing seed data. 10 tests.
- [x] `calculateReadinessScore(programId)` — 5 equal-weighted (20 pts each) factors: verification coverage, test health, milestone health, defect health, risk health; missing data scores a factor as neutral (full 20 pts) with a warning, never as a penalty; rounded total clamped to [0, 100]. EdgeLink-X's exact seeded score (56/100) locked in as a regression test, with each factor's sub-score independently asserted. 6 tests.
- [x] `buildAnalysisEvidence(eventId)` — composes every above service; evidence items are `{ recordId, recordType, summary }` built only from validated fields; `ProgramEvent.rawNotes` (the seeded prompt-injection sentence) is exposed only as a separate `untrustedSupplierNotes` field, never inside `evidence[]`, and never read by any calculation. Deduplicated by `recordType+recordId`, deterministically ordered. For the seeded `EVT-SUPPLIER-001`, all 11 `EVIDENCE_RECORD_TYPES` categories are present with exactly the expected record IDs, and unrelated records (other suppliers, battery's risk/requirement/budget item, other events) are proven absent. 10 tests.
- [x] Test infrastructure: `packages/core/src/test/setup-env.ts` (new Vitest `setupFiles` entry) force-loads `.env.test` and hard-fails the whole run if `DATABASE_URL` doesn't resolve to the approved local test target — database-backed Phase 2 tests can never accidentally run against `missionthread_dev`. A handful of tests create small, uniquely-IDed temporary fixtures (isolated component, temp programs, temp events) for scenarios the standard seed doesn't naturally cover, always cleaned up in `afterAll`/`finally`.
- [x] 95 new tests added (134 → 229 total), all passing; no existing Phase 1 test weakened or removed.
- [x] Fixed an unrelated, pre-existing local environment issue discovered while typechecking: an apparent file-sync tool had left hundreds of `"<name> 2"`-suffixed duplicate files inside `node_modules` and `apps/web/.next` (both fully regenerable, gitignored build artifacts) on the local machine, breaking `tsc`. Removed and regenerated; not a code or repository issue.
- [x] Re-ran the full quality gate: install, `db:validate`, `db:generate`, lint, format check, typecheck (all 3 workspaces), 229 unit tests, production build, `db:reset:test` + full retest against the freshly reset database, smoke test — all passing.
- [x] Updated `docs/ARCHITECTURE.md` (new "Deterministic program-analysis services" section) and `README.md` (project status, roadmap table, architecture summary) to reflect Phase 2 completion.

## Phase 2 correction pass — done (2026-07-18)

A narrowly-scoped review confirmed two real, reproducible functional blockers in the Phase 2 build (not yet caught because CI had never actually run these tests). Full disposition and detail is in `docs/DECISIONS.md`'s "(Phase 2 correction pass)" entries. Summary of what changed:

- [x] **Blocker 1 fixed:** the Vitest database-selection logic only ever validated against the local `(host, port, database)` tuples, so every database-backed Phase 2 test would have failed the first time CI actually ran them (CI's Postgres service is `localhost:5432`, not the local `localhost:55432`). Extracted a pure, DB-free `resolveTestDatabaseConfiguration(env)` (`packages/core/src/test/resolve-test-database-configuration.ts`) that selects the GitHub Actions context (`GITHUB_ACTIONS=true`, never touches `.env.test`, validates against `GITHUB_ACTIONS_TEST_TARGETS`) or the local context (loads `.env.test` with `override: true`, validates against `LOCAL_TEST_TARGETS`) — no allowlist was broadened. 18 new unit tests, no database connection or file I/O in any of them.
- [x] **Blocker 1, part 2 fixed:** `.github/workflows/ci.yml` ran unit tests _before_ migrating and seeding the CI database — reordered to install → schema validation → Prisma generation → lint → format check → type check → migrate → seed → tests → build → smoke test. Verified live locally by simulating both `GITHUB_ACTIONS=true` (correct CI target passes) and wrong-port/CI-only-without-GITHUB_ACTIONS (correctly rejected) scenarios.
- [x] **Blocker 2 fixed:** `buildAnalysisEvidence()` was discarding the actual structured output of every sub-service (`VerificationGapsResponse`, `RelatedDefectsResponse`, `ScheduleExposureResult`, `BudgetExposureResult`, `ReadinessScoreResult`) and never called `calculateRiskScore()` at all for risk evidence (a hand-rolled query kept only `severity`/`status`, discarding probability, impact, numeric score, computed band, and the severity-consistency flag). `AnalysisEvidence` now carries the complete result of every sub-service directly — `eventFacts`, `impactedRequirements`, `impactedMilestones`, `verificationGaps`, `relatedDefects`, `scheduleExposure`, `budgetExposure`, `riskScores`, `readinessScore` — reusing each service's own public type, alongside the existing bounded `evidence[]`/`assumptions`/`unknowns`.
- [x] `eventFacts.computedDelayDays`/`delayDaysConsistent` are now populated directly from `calculateScheduleExposure()`'s own authoritative result, not a separate read of `event.delayDays`; `storedDelayDays` is still reported alongside it so a disagreement stays visible.
- [x] `event.reason` (previously embedded directly in the trusted event-evidence summary) now joins `event.rawNotes` as untrusted free text — both isolated in a new `untrustedText: { reason, rawNotes }` field, replacing `untrustedSupplierNotes`, never read by any calculation, never in `evidence[]`.
- [x] Added explicit, documented evidence bounds (`EVIDENCE_LIMITS`: 100 total items, 25 per record type, 500-character summaries, 4,000-character untrusted-text fields) via pure, exported `applyEvidenceBounds()`/`truncateText()` — deterministic ordering preserved, every truncation produces an `unknowns` entry, and truncation is proven surrogate-pair-safe (never splits a UTF-16 astral character, e.g. an emoji, in half).
- [x] `evidence.test.ts` rewritten and expanded (10 → 26 tests): exact seeded values for schedule/budget/verification/defects/risk/readiness, a dedicated trust-boundary suite proving the supplier-injection phrase (and `event.reason`'s text) appears only in `untrustedText` and nowhere else, and bounds tests (summary/item-limit truncation, surrogate-pair safety, deterministic repeatability) using synthetic data.
- [x] No existing Phase 2 service (`ServiceResult<T>`, input validation, BFS traversal, cycle/duplicate protection, decimal-safe budget arithmetic, schedule inconsistency detection, risk-score formula, readiness formula, evidence deduplication/ordering) was redesigned or weakened — only the evidence _composition_ layer changed.
- [x] 34 new tests added (229 → 263 total), all passing; no existing test weakened or removed.
- [x] Re-ran the full quality gate: install, `db:validate`, `db:generate`, `db:reset:test`, lint, format check, typecheck (all 3 workspaces), 263 unit/integration tests, production build, smoke test — all passing. Confirmed local tests use only `localhost:55432/missionthread_test`.

## Phase 3 — Core workflow UI — done (2026-07-18)

Real database-driven dashboard, program overview, event entry, and audit shell. Server-side authorization on the only mutation this phase performs. Full detail is in `docs/DECISIONS.md`'s "(Phase 3)" entries.

- [x] **Genuine CI blocker fixed first (exception clause):** the smoke-test step had been silently hanging for GitHub's full 6-hour job timeout on multiple prior pushes, with no failure signal. Root-caused to `npx`-wrapped process spawning likely not reliably propagating `SIGTERM`; fixed by spawning the `next` binary directly (resolved via Node's own module resolution, not a hardcoded path), a `SIGKILL` fallback, an explicit `process.exit()`, and a 90-second watchdog that force-fails loudly instead of hanging silently, regardless of exact cause.
- [x] Event-entry contract: `packages/core/src/events/schemas.ts` — strict Zod discriminated union (`SUPPLIER_DELAY` / `GENERAL_UPDATE`) keyed by `eventType`; `YYYY-MM-DD` dates validated for both format and real calendar validity; `revisedDate > originalDate` enforced; documented bounds (quantity ≤ 100,000; reason ≤ 500 chars; rawNotes ≤ 4,000 chars); unknown fields rejected, including a client-supplied `delayDays` (never part of the contract). 40 tests.
- [x] `recordProgramEvent(input, actorUserId)` (`packages/core/src/events/record-program-event.ts`): validates input, re-fetches the actor's role from the database on every call (never trusts a session/JWT claim), verifies component/supplier belong to `PROGRAM-EDGELINK-X` (a nonexistent ID and an ID from another program produce the identical `NOT_FOUND`, never distinguished), computes `delayDays` server-side via the same `utcDayDifference()` Phase 2 already uses, and creates the `ProgramEvent` + one matching `EVENT_RECORDED` `AuditEvent` in a single Prisma transaction. Extended the Phase 2 error strategy with a `FORBIDDEN` code rather than inventing a second one. 14 tests, including a live-forced-FK-violation rollback proof.
- [x] `apps/web/src/lib/auth-helpers.ts` gained `requireProgramManager()` — a page-level UX check (redirects a non-manager away before the event-entry form renders) that independently re-verifies the database role, kept deliberately separate from `recordProgramEvent()`'s own authoritative check.
- [x] Executive dashboard (`apps/web/src/app/(app)/page.tsx`): readiness score + factor breakdown, requirement count, verification-gap count, milestone count, at-risk/delayed milestone count, open risk count, open/in-progress defect count, budget planned/actual/variance, recent events, latest supplier-delay schedule exposure, links to overview and (Program-Manager-only) event entry. Every failed Phase 2 service call shows an explicit "unavailable" state, never an invented zero.
- [x] Program overview (`.../programs/edgelink-x/page.tsx`): components, requirements with component traceability and verification-gap badges, milestones, dependency edges, risk register, test outcomes, open defects, budget items and variance, suppliers (name only — contact deliberately not selected), recent events with untrusted supplier notes clearly labeled and rendered as plain text.
- [x] Event-entry page (`.../events/new/`): server action + client form, `SUPPLIER_DELAY`/`GENERAL_UPDATE` toggle showing only relevant fields, accessible labels with `aria-describedby` error associations, disabled-while-pending submit, cancel link, no client-supplied actor/program controls, redirects to the overview with a visible success banner on success.
- [x] Audit shell (`/audit`): real `AuditEvent` rows, filters (`action`/`actorType`/`targetType`/`traceId`) validated against Zod enum allowlists (`AUDIT_ACTIONS`/`AUDIT_ACTOR_TYPES` added to `packages/core/src/record-types.ts` alongside the existing `AUDIT_TARGET_TYPES`) with invalid values silently ignored rather than erroring the page, `createdAt` desc then `id` desc ordering, a hard 50-row cap, active-filter chips, and a reset-filters link.
- [x] Nav: `Record event` link shown only when the session role is `PROGRAM_MANAGER` — the real security boundary remains `recordProgramEvent()`'s own database re-check, not this link's visibility.
- [x] Deterministic `EVENT_RECORDED` audit seed fixtures (`AUDIT_EVENT_IDS`/`AUDIT_TRACE_IDS`, `seedAuditEvents()`) — one per seeded `ProgramEvent`, fixed IDs/trace IDs/timestamps, the seeded Program Manager as actor, structured `afterValue` payloads mirroring `recordProgramEvent()`'s own redaction shape.
- [x] Caught and fixed a real bug via the extended smoke test itself: a `"use server"` file exporting a plain constant (Next.js only preserves async-function exports from such files) silently became `undefined` at the client call site, crashing the event-entry page on every load. Fixed by moving the constant to the client component that uses it.
- [x] Smoke test extended: dashboard/overview/audit real-content checks, Program-Manager access to the event-entry page, Engineering-Lead redirect-away from it, unauthenticated redirect coverage for the new route, sign-out — 27 → 41 checks, all passing.
- [x] 54 new tests added (263 → 317 total; 40 schema + 14 mutation), all passing; no existing test weakened.
- [x] Manually verified against `missionthread_dev` (not `missionthread_test`): all three seeded roles can view the dashboard, overview, and audit pages; only the Program Manager can reach the event-entry page (Engineering Lead and Executive Viewer are both redirected away and both independently rejected with `FORBIDDEN` at the `recordProgramEvent()` level); a Program-Manager-submitted event produces exactly one `ProgramEvent` and one matching `AuditEvent` sharing a trace ID (verified, then cleaned up); zero `ImpactAnalysis`/`MitigationOption`/`ProposedChange`/`Decision` rows exist anywhere.
- [x] Re-ran the full quality gate: install, `db:validate`, `db:generate`, `db:reset:test`, lint, format check, typecheck (all 3 workspaces), 317 unit/integration tests, production build, smoke test (41/41) — all passing. Confirmed local tests use only `localhost:55432/missionthread_test`; no local destructive operation targeted port `5432`.
- [x] Deferred Phase 4 constraint recorded in `docs/DECISIONS.md`: a future `buildAnalysisEvidence()` caller must construct an explicit bounded model-input projection, never serialize the whole `AnalysisEvidence` object (only `evidence[]` itself is bounded by `EVIDENCE_LIMITS`).

## Phase 3 correction pass — done (2026-07-19)

A narrowly-scoped review confirmed one real, reproducible defect: the event-entry server-action adapter silently defaulted any `eventType` other than the exact string `SUPPLIER_DELAY` to `GENERAL_UPDATE`, so malformed values (`"SOMETHING_ELSE"`, `""`, missing, a non-string `FormData` entry) were converted into a valid general-update candidate before `eventEntrySchema` ever validated them — the core schema's own rejection logic was correct and untouched; the adapter in front of it just never let it see the actual bad value. Full detail in `docs/DECISIONS.md`'s "(Phase 3 correction pass)" entry.

- [x] Extracted `buildEventEntryInputFromFormData()` into a new pure module, `apps/web/src/app/(app)/programs/edgelink-x/events/new/form-input.ts`: three explicit branches (`SUPPLIER_DELAY`, `GENERAL_UPDATE`, and a fallthrough that preserves the actual invalid/missing discriminator for `eventEntrySchema` to reject, never substituting `GENERAL_UPDATE`). `actions.ts` now imports and calls it instead of containing the branching inline.
- [x] Added a minimal Vitest setup to `apps/web` (`vitest.config.ts`, `test` script, `vitest` devDependency — mirroring `packages/core`'s existing configuration) specifically to unit-test this pure module; no database, no `setupFiles`. `apps/web/scripts/smoke-test.mjs` remains the only coverage for page rendering and database-backed behavior.
- [x] 15 new tests: exact `SUPPLIER_DELAY`/`GENERAL_UPDATE` candidates built correctly; `SOMETHING_ELSE`, missing, empty, and non-string (`File`) discriminators all preserved (or reduced to `""`) rather than becoming `GENERAL_UPDATE`; a parametrized case/whitespace-variant sweep (`"supplier_delay"`, `"GENERAL_UPDATE "`, etc.) proving none of them produce a general-update candidate; valid submissions still parse successfully; a named regression test reproducing the exact `eventType=SOMETHING_ELSE, rawNotes=<valid text>` input that previously passed as `GENERAL_UPDATE` before this fix. No existing core schema test (`packages/core/src/events/schemas.test.ts`) was touched.
- [x] Unchanged, verified: `recordProgramEvent()`'s authorization/relationship checks, the event/audit transaction, `AuditEvent` payload redaction, dashboard/overview/audit rendering and data sources, and every other Phase 2/3 deterministic service.
- [x] Re-ran the full quality gate: install, `db:validate`, `db:generate`, lint, format check, typecheck (all 3 workspaces), test (317 core + 15 new web = 332 total), production build, smoke test (41/41) — all passing. Confirmed the new pure-parser tests make zero database queries (test-database row counts unchanged before/after); zero `ImpactAnalysis`/`MitigationOption`/`ProposedChange`/`Decision` rows anywhere.
- [x] Checked GitHub Actions status before starting: the prior push's CI run completed (not hung — the Phase 2 correction pass's smoke-test hang fix is confirmed working in the real CI environment) but failed at the smoke-test step on an unrelated, pre-existing defect — Auth.js `UntrustedHost` errors in the GitHub Actions runner environment specifically (`AUTH_TRUST_HOST`/`AUTH_URL` not configured for that context), causing every authenticated smoke check to fail there even though the same suite passes locally. Explicitly out of scope for this pass (which forbids modifying authentication) — recorded here as a known, currently-unfixed CI risk for a future pass to address.

## Phase 3 CI repair — done (2026-07-19)

Fixed exactly the CI `UntrustedHost` risk flagged at the end of the previous correction pass. Full detail in `docs/DECISIONS.md`'s "(Phase 3 CI repair)" entry.

- [x] Added `AUTH_TRUST_HOST: "true"` to the `env:` block of the `Application smoke test` step only in `.github/workflows/ci.yml` — not at workflow scope, not inside `apps/web/src/auth.ts`. Non-secret CI runtime setting; no new GitHub secret needed.
- [x] Tested the minimal fix first, per instructions, without speculatively also adding `AUTH_URL` — that addition is deferred unless an actual GitHub Actions run still shows a callback/origin/host-resolution error after this change.
- [x] Every other CI boundary confirmed unchanged: `permissions: contents: read`, the Postgres service and `GITHUB_ACTIONS_TEST_TARGETS` tuple, `AI_MODE=mock`, migrate-then-seed-then-test ordering, per-step destructive authorization, the direct `next` binary spawn, `SIGTERM`/`SIGKILL` handling, the 90-second watchdog, and all 41 smoke-test assertions.
- [x] Re-ran the full local quality gate: install, `db:validate`, `db:generate`, `db:reset:test`, lint, format check, typecheck (all 3 workspaces), test (317 core + 15 web = 332 total), production build, smoke test (41/41) — all passing. Confirmed zero `ImpactAnalysis`/`MitigationOption`/`ProposedChange`/`Decision` rows and an isolated, unaffected test database (unchanged seeded row counts before/after).
- [x] Verified end-to-end: the push-triggered GitHub Actions run (`29705561052`) completed with `status: completed`, `conclusion: success`, confirmed via `gh run view 29705561052 --json status,conclusion,headSha` and matching the pushed commit's SHA — the CI-specific `UntrustedHost` failure is resolved, not merely fixed locally.

## Phase 4 — AI impact analysis — done (2026-07-20)

Full AI impact-analysis pipeline: provider abstraction, mock and live
adapters, bounded model-input projection, strict structured-output
validation, semantic/source validation, one-retry failure path, persisted
attempts with `analysisRunId` grouping, structured logging, analysis
workspace UI, printable readiness briefing, and a seeded demonstration
analysis. Full disposition is in `docs/DECISIONS.md`'s "(Phase 4)" entries
and `docs/ARCHITECTURE.md`'s "AI — implemented (Phase 4)" section.

- [x] Prisma migration `20260719000000_add_analysis_run_tracking`: `ImpactAnalysis` gained `analysisRunId`, `requestedById` (User relation), `validationPassed`, `validationErrors`, `@@unique([analysisRunId, attempt])`, `@@index([analysisRunId])`, `@@index([requestedById])` — generated non-interactively via `prisma migrate diff` against a throwaway shadow database (this environment has no interactive TTY for `prisma migrate dev`), applied to `missionthread_dev` and, later, `missionthread_test` via the existing `db:reset:test` command under fresh per-invocation authorization.
- [x] `packages/core/src/ai/`: `provider.ts` (`LLMProvider`/`LLMProviderRequest`/`LLMProviderResponse`), `errors.ts` (`AiConfigurationError`, `AiProviderError`, safe error-category allowlist), `provider-factory.ts` (strict `AI_MODE` resolution, `createProviderFromEnv()`), `mock-provider.ts` (pure `generateMockImpactAnalysis()` + `MockLLMProvider`), `openai-provider.ts` (Responses API, live mode only), `model-input.ts` (`buildModelInputProjection()`, bounds, size check), `output-schema.ts` (authoritative Zod output schema), `output-validation.ts` (semantic/source validation), `orchestrator.ts` (`runImpactAnalysis()`), `logging.ts` (structured JSON logs), `prompts/impact-analysis-system.ts`, `prompts/impact-analysis-user.ts`, `index.ts`.
- [x] `buildModelInputProjection()`: never serializes the full `AnalysisEvidence`; explicit bounds (`MODEL_INPUT_LIMITS`, reusing `EVIDENCE_LIMITS.maxItemsPerRecordType`) on every previously-unbounded collection; deterministic ordering preserved; every truncation recorded as an `unknowns` note, never silently dropped; final serialized-size ceiling check before any provider call.
- [x] Authoritative Zod output schema (`impactAnalysisOutputSchema`): `.strict()` throughout, fixed 3-tuple `mitigationOptions` with exactly one `isRecommended: true` (enforced via `.refine()`), fixed-2-decimal monetary strings, documented length/array limits, no optional fields.
- [x] Semantic validation (`validateImpactAnalysisSemantics()`): every `sourceRecordIds` entry (top-level and per-option) must exist in the request's own evidence allowlist; `affectedRequirementIds`/`affectedMilestoneIds` must exist as `REQUIREMENT`/`MILESTONE` evidence; `scheduleExposureDays` must exactly equal `ScheduleExposureResult.directDelayDays`; `budgetExposureAmount` must exactly equal `BudgetExposureResult.totalDeterministicExposure`; duplicate-ID checks. Mapping documented precisely in `docs/DECISIONS.md` per the task's explicit instruction.
- [x] Mock provider: deterministic, no network, no API key; 3 options/1 recommended; every citation from the supplied evidence allowlist; every monetary/schedule figure either `null` or the deterministic value already in the input — never invented.
- [x] Live provider: official `openai` npm package, Responses API, strict JSON-schema structured output generated from the authoritative Zod schema via `z.toJSONSchema()` (verified directly against Zod 4.4.3 before use, not assumed), `store: false`, no streaming/tools/web-search/conversations/previous-response-IDs. No test, integration test, smoke test, or CI step calls it — verified by direct code review of every test file.
- [x] Orchestration (`runImpactAnalysis(eventId, actorUserId, options?)`): re-verifies actor's current database role (`PROGRAM_MANAGER` only) fresh from the database on every call; builds evidence + validated model input; attempt loop (max 2) — `PENDING` row + `ANALYSIS_STARTED` audit event, provider call outside any transaction, structural then semantic validation, `SUCCEEDED` (one transaction, exactly 3 options) or `FAILED` (safe error category + validation errors only) persistence; retry exactly once on a retryable category with concise feedback; configuration failures never retried and create zero rows for a pre-attempt failure.
- [x] Structured logging (`logAnalysisEvent()`): one JSON line per lifecycle event, injectable sink, never a secret/prompt/raw-output/credential.
- [x] Web: Program-Manager-only "Analyze" control on `/programs/edgelink-x`'s Recent Events section (`analyze-button.tsx`, `actions.ts` — actor ID from session only, authorization re-check relied on entirely from `runImpactAnalysis()`); analysis workspace at `/programs/edgelink-x/analyses/[id]` (all roles view; run status, every attempt, deterministic facts, evidence citations, 3 mitigation options); printable readiness briefing at `/programs/edgelink-x/briefings/[id]` (print-specific CSS hides nav/interactive controls; safe non-briefing state for a pending/failed run).
- [x] Seeded demonstration analysis for `EVT-SUPPLIER-001`: calls the real production `buildAnalysisEvidence()` → `buildModelInputProjection()` → `generateMockImpactAnalysis()` pipeline at seed time and re-validates the result against the production schema/semantic checks before persisting — not a hand-authored duplicate shape. Fixed IDs added to `packages/core/src/seed/ids.ts`. Found and fixed a real ESM import-ordering bug while building this (dynamic imports needed inside `seedImpactAnalysis()` — see `docs/DECISIONS.md`).
- [x] Tests: 41 new AI test files/suites (`model-input.test.ts`, `output-schema.test.ts`, `output-validation.test.ts`, `mock-provider.test.ts`, `provider-factory.test.ts`, `logging.test.ts`, `orchestrator.test.ts`) covering model-input bounds/ordering/untrusted-text isolation, output-schema structural rules, semantic validation (fabricated IDs, wrong record type, deterministic mismatches, duplicates), mock-provider determinism/validity, and orchestration (authorization, attempt lifecycle, retry, `analysisRunId` grouping, distinct trace IDs, independent re-analysis, configuration-failure non-retry, audit actions, source-reference deduplication, safe error persistence). 74 new tests (317 → 391 core tests), no existing test weakened. No test calls a live provider.
- [x] Smoke test extended: seeded analysis workspace and briefing return 200; analysis page shows the seeded trace ID; exactly 3 mitigation-option cards and exactly 1 recommended badge (via `data-testid`, not raw text-occurrence counting — Next's RSC flight payload duplicates rendered text for hydration, which broke an initial raw-count assertion; fixed by adding `data-testid="mitigation-option"`/`"mitigation-recommended-badge"` and a `countTestId()` helper); briefing shows confidence/assumptions/unknowns/references; unauthenticated access to both new routes redirects to `/login`; Engineering Lead can view the analysis but sees no Analyze control. 19 new checks (41 → 60), all passing, watchdog unaffected.
- [x] Docs: `.env.example`/`.env.test.example` (`AI_MODE` strict values, `OPENAI_API_KEY`/`OPENAI_MODEL` documented with no real key), `README.md` (status, architecture, routes, mock-vs-live AI, limitations, roadmap), `docs/ARCHITECTURE.md` (full "AI" and "Observability" sections rewritten from "planned" to "implemented", request/data flow diagram updated, new analysis-workspace/briefing UI section), `docs/DECISIONS.md` (8 new "(Phase 4)" entries), this file. Corrected the stale Phase 3 CI-repair entry above to record that GitHub Actions run `29705561052` passed end-to-end.
- [x] Quality gate: fresh `AskUserQuestion` authorization obtained before each of the two destructive database operations this pass performed (`db:seed:dev:destructive`, `db:reset:test`); `node -v`, `npm ci`-equivalent state, `db:validate`, `db:generate`, `db:reset:test`, `lint`, `format:check`, `typecheck` (all 3 workspaces), `test` (391 core + 15 web = 406), `build`, `smoke:test` (60/60) all passing. Confirmed: mock mode needs no API key; no test/CI step calls the live provider; test DB isolated (`localhost:55432/missionthread_test`); zero `Decision`/`ProposedChange` rows anywhere; every successful analysis has exactly 3 mitigation options with exactly 1 recommended; every failed analysis has zero; CI (`.github/workflows/ci.yml`) still sets `AI_MODE: mock` at workflow scope and requires no OpenAI env vars.

## Phase 4 correction pass — done (2026-07-21)

Three confirmed Phase 4 blockers fixed: a live-provider JSON Schema incompatibility, incomplete attempt-evidence persistence, and a non-immutable readiness figure on historical briefings. Full disposition is in `docs/DECISIONS.md`'s "(Phase 4 correction pass)" entries.

- [x] **JSON Schema fix:** `impactAnalysisOutputSchema.mitigationOptions` changed from `z.tuple([...])` (converts to JSON Schema `prefixItems`, outside OpenAI Structured Outputs' supported subset) to `z.array(mitigationOptionOutputSchema).length(3)` (converts to `items`/`minItems: 3`/`maxItems: 3`, all supported) — exactly-three-options and exactly-one-recommended enforcement unchanged. New `openai-schema.ts`: `buildOpenAiImpactAnalysisJsonSchema()` generates from the authoritative schema and recursively verifies no disallowed keyword (`prefixItems`, `unevaluatedItems`, `contains`, `minContains`, `maxContains`, `propertyNames`, `patternProperties`) appears anywhere, and every object schema has `additionalProperties: false` with every property in `required`. `openai-provider.ts` now uses this helper instead of an inline `z.toJSONSchema()` call. Live response still always re-validated against the authoritative Zod schema.
- [x] **Complete attempt-evidence persistence:** `SourceReference` gained `wasCited Boolean @default(false)` and `citationContexts Json?` (migration `20260720000000_add_evidence_snapshot_and_readiness_snapshot`). New pure `buildAttemptSourceReferenceSnapshot(modelInput, output?)` (`attempt-persistence.ts`) builds the complete supplied-evidence snapshot (no `output`) or adds citation metadata (`output` present). `orchestrator.ts` restructured: a new `persistPendingAttempt()` persists the `PENDING` row, the full evidence snapshot, and `ANALYSIS_STARTED` in one transaction before the provider is ever called; `persistSucceededAttempt()` now updates (never re-creates) the cited subset's rows. Failed attempts and each retry attempt independently retain their own complete snapshot. `prisma/seed.ts` uses the same shared helper — no separate cited-only seed implementation.
- [x] **Cited vs. supplied-only evidence in the UI:** analysis workspace's evidence section renamed "Evidence citations" → "Evidence supplied to analysis," each record tagged "Cited (‹contexts›)" or "Supplied only." Briefing's "Source references"/"Relevant risks" sections filtered to cited-only (a decision document showing what was used, not everything supplied).
- [x] **Immutable readiness snapshot:** `ImpactAnalysis` gained `readinessSnapshot Json?` (same migration). `readinessSnapshotSchema` exported from `model-input.ts` — reused as both the model-input and the persisted-snapshot content schema. `buildSucceededImpactAnalysisData()` persists `modelInput.deterministicResults.readinessScore` directly (`Prisma.DbNull` when unavailable), never recalculated. Briefing's `calculateReadinessScore(PROGRAM_ID)` call removed entirely; analysis workspace gained a new "Program readiness at analysis time" section. Verified with a real historical-immutability integration test: run an analysis, close a risk (confirmed this changes current program readiness), reload the analysis, prove its stored snapshot is unchanged, restore the risk.
- [x] Deterministic values (`scheduleExposureDays`/`budgetExposureAmount`/`readinessSnapshot`) in the success transaction now explicitly sourced from `modelInput.deterministicResults`, never the model's own output copy.
- [x] Runtime `modelInputProjectionSchema.safeParse()` validation added in `orchestrator.ts`, immediately after `buildModelInputProjection()` and before any attempt is created — a failure returns a safe validation error with zero rows/audit events/provider calls.
- [x] Migration `20260720000000_add_evidence_snapshot_and_readiness_snapshot`, generated non-interactively (throwaway shadow database, same mechanism as the prior Phase 4 migration), applied to `missionthread_dev` and `missionthread_test`.
- [x] 41 new tests (`openai-schema.test.ts`, `openai-provider.test.ts`, `attempt-persistence.test.ts`, `readiness-snapshot.test.ts`, extended `orchestrator.test.ts`) — schema-shape verification, live-request-construction inspection against a fake client, pure snapshot-builder behavior, historical-immutability, complete/uncited-evidence persistence, failed/retry-attempt snapshot retention, no-untrusted-text-in-persistence. 441 core tests total (419 → 441), 15 web unchanged, 456 combined. No test calls a live provider.
- [x] Docs: `docs/DECISIONS.md` (8 new "(Phase 4 correction pass)" entries), `docs/ARCHITECTURE.md` (AI section and analysis-workspace/briefing UI section updated), `README.md`, this file.
- [x] Quality gate: fresh `AskUserQuestion` authorization obtained before each destructive database operation this pass performed (`db:seed:dev:destructive`, `db:reset:test`); `node -v`, `db:validate`, `db:generate`, `db:reset:test`, `lint`, `format:check`, `typecheck` (all 3 workspaces), `test` (441 core + 15 web = 456), `build`, `smoke:test` (60/60) all passing. Confirmed: no test/CI step calls the live provider; CI still `AI_MODE=mock`; every successful analysis still has exactly 3 mitigation options with exactly 1 recommended, every failed analysis zero; zero `Decision`/`ProposedChange` rows anywhere; Phase 1–3 functionality unchanged.

## Phase 4 persistence-boundary repair — done (2026-07-22)

One confirmed defect fixed at two boundaries: the output schema didn't match the actual persisted-column limits, and a persistence failure after a valid provider response was misclassified as a retryable provider failure. Full disposition is in `docs/DECISIONS.md`'s "(Phase 4 persistence-boundary repair)" entries.

- [x] **Database-safe output constraints:** `output-schema.ts` gained `MAX_DECIMAL_12_2_INTEGER_DIGITS` (10) and exported `persistedMoneyStringSchema` (`/^\d{1,10}\.\d{2}$/`), replacing the unbounded money regex for `budgetExposureAmount` and every mitigation option's `costImpact` — matches PostgreSQL `Decimal(12,2)`'s actual capacity. Added `MIN_MITIGATION_SCHEDULE_IMPACT_DAYS`/`MAX_MITIGATION_SCHEDULE_IMPACT_DAYS` (±3650) for `mitigationOptions[*].scheduleImpact` specifically (not `scheduleExposureDays`, which is deterministic and checked for exact equality elsewhere). No migration needed — only the Zod gate was tightened, not the unchanged database columns. The provider-facing JSON Schema automatically carries the resulting `pattern`/`minimum`/`maximum` (`z.toJSONSchema()`, no code change needed in `openai-schema.ts`), verified directly.
- [x] **Provider vs. persistence failure separation:** the attempt loop is now five explicit stages (pending-attempt persistence, provider invocation, structural validation, semantic validation, success persistence). A new `runProviderAndValidate()` helper's `try/catch` covers only the provider call; structural/semantic validation happen outside it (they never throw). `persistSucceededAttempt()` now runs in its own separate `try/catch`, never inside the provider stage.
- [x] **New non-retryable `PERSISTENCE_FAILURE` category** added to `AI_ERROR_CATEGORIES` (`errors.ts`), deliberately excluded from `RETRYABLE_CATEGORIES`.
- [x] **Successful-output persistence failure:** the provider is never called again, `analysis.retrying` is never logged, the attempt is marked `FAILED`/`PERSISTENCE_FAILURE` through a separate persistence call, `ANALYSIS_FAILED` is created only if that succeeds, the evidence snapshot from the (already-committed) pending-attempt transaction remains, zero `MitigationOption` rows survive. If even the FAILED-marking call fails, returns a generic safe infrastructure error rather than claiming a provider failure or a false recording.
- [x] **Pending-attempt persistence failure:** provider never called, no attempt counted, no partial rows (real `prisma.$transaction` atomicity, verified with a genuine primary-key collision, not just a mock).
- [x] **`AnalysisPersistence` interface + `defaultAnalysisPersistence`:** narrow test-only override point on `runImpactAnalysis`'s existing `options` parameter (`options.persistence`) — same shape as the pre-existing `options.provider` override. `apps/web` never passes `options`, so this is unreachable from the web client. No global Prisma mocking used anywhere.
- [x] 27 new/extended tests: database-safe boundary values (money digit-count, schedule-impact range), generated-schema `pattern`/`minimum`/`maximum` assertions, explicit retry-boundary re-verification (transient/malformed/semantic/config, exact call counts), the full persistence-boundary scenario (provider called exactly once, no retry logged, `PERSISTENCE_FAILURE`, zero options, evidence retained, injected secret text absent from every observable output), pending-attempt real-transaction-rollback proof. 468 core tests total (441 → 468), 15 web unchanged, 483 combined. No test calls a live provider.
- [x] Docs: `docs/DECISIONS.md` (3 new "(Phase 4 persistence-boundary repair)" entries), `docs/ARCHITECTURE.md` (output-schema and orchestration sections updated), `README.md`, this file.
- [x] Quality gate: fresh `AskUserQuestion` authorization obtained before the destructive database operation this pass performed (`db:reset:test`); `node -v`, `npm ci`, `db:validate`, `db:generate`, `db:reset:test`, `lint`, `format:check`, `typecheck` (all 3 workspaces), `test` (468 core + 15 web = 483), `build`, `smoke:test` (60/60) all passing. Confirmed: no test/CI step calls the live provider; CI still `AI_MODE=mock`; a successful provider response followed by a persistence failure never triggers a second provider call; failed analyses create zero options; full attempt evidence remains available; historical readiness remains immutable; zero `Decision`/`ProposedChange` rows anywhere.

## Phase 5 — Approval and audit — done (2026-07-23)

Full approval/apply workflow: decision state machine, role-based decision
permissions, server-generated proposed-change snapshots, apply-preview
page, explicit apply confirmation, transactional all-or-nothing apply,
stale-data conflict detection, append-only audit, integration tests, and a
Playwright happy-path test. Full disposition is in `docs/DECISIONS.md`'s
"(Phase 5)" entries and `docs/ARCHITECTURE.md`'s "Approval and apply
workflow — implemented (Phase 5)" section.

- [x] Migration `20260722000000_phase5_decision_state_machine`: `Decision.mitigationOptionId` gained `@unique` (`MitigationOption.decision` is now `Decision?`, not an array), `Decision.rationale` became required, `ProposedChange.targetRecordId`/`targetRecordType` became nullable (`NEW_ACTION` has no existing target) — generated via the established non-interactive shadow-database `migrate diff` technique, applied to `missionthread_dev` and `missionthread_test`.
- [x] New `CONFLICT` `DomainErrorCode` + `conflict()` constructor in `packages/core/src/analysis/types.ts` — a well-formed request against a record that exists, but where proceeding would silently overwrite a decision that already happened or a change nobody approved.
- [x] `packages/core/src/approvals/`: `schemas.ts` (`recordDecisionInputSchema` — strict Zod discriminated union keyed by `verdict`; `proposedChangeInputSchema` — 4-way discriminated union keyed by `changeType`), `snapshot.ts` (`buildProposedChangeSnapshot()` — server-generated old/new values, never client-trusted), `stale.ts` (`checkProposedChangeStale()` — normalized comparison shared between preview and apply), `record-decision.ts` (`recordMitigationDecision()`), `apply-changes.ts` (`applyApprovedChanges()`), `index.ts`.
- [x] Decision state machine: `PENDING → APPROVED | REJECTED | REVISION_REQUESTED`, no transition from a terminal state, at most one decision per option — enforced by the database (`Decision.mitigationOptionId @unique`), not just application logic. A genuine concurrent-decision race is closed by the unique-constraint violation (caught, reported as `CONFLICT`), verified directly with two simultaneous decision calls on one option.
- [x] Decision permissions: Program Manager (approve/reject/request-revision/apply), Engineering Lead (request-revision only), Executive Viewer (read-only) — revalidated from the database on every call, identical pattern to `recordProgramEvent()`/`runImpactAnalysis()`. Verified with a role-change-between-calls test.
- [x] Proposed-change discriminated union: `MILESTONE_DATE` (target must be a `Milestone`, new `currentDate`), `RISK_UPDATE` (target must be a `Risk`, allowlisted writable fields `status`/`severity`/`probability`/`impact`, at least one required), `BUDGET_UPDATE` (target must be a `BudgetItem`, `plannedAmount`/`actualAmount` reusing `persistedMoneyStringSchema`, at least one required, no currency changes), `NEW_ACTION` (`targetRecordId`/`targetRecordType` required literal `null`, bounded `{ title, description, dueDate }` payload).
- [x] Server-generated snapshots: `buildProposedChangeSnapshot()` loads the target, verifies `PROGRAM-EDGELINK-X` membership, builds `oldValue` from the current database row and `newValue` from validated client input — the client's own claimed old value, target type, program ID, status, and `appliedAt` are never trusted or persisted. `NEW_ACTION`'s safe `oldValue` is always `{}`; its durable record is the applied `ProposedChange` row itself (no separate `ActionItem` model), surfaced in a new program-overview "Actions" section.
- [x] Decision transaction: actor/permission check, option/program lookup, PENDING/no-existing-decision checks, proposed-change validation and snapshotting, `Decision` creation, status transition, `ProposedChange` creation (approval only), one `DECISION_RECORDED` `AuditEvent` — all atomic. Audit payload is bounded and safe (verdict, IDs, change types, `hasRationale` boolean); the full rationale never leaves the `Decision` row. A forced mid-sequence failure (an unknown proposed-change target) rolls back everything, verified directly.
- [x] Stale-data conflict detection: `checkProposedChangeStale()` reused identically at apply-preview render time (read-only) and inside `applyApprovedChanges()`'s own transaction, using the same normalized representation (UTC date-only strings, fixed-two-decimal monetary strings, Prisma enum strings) the snapshot was built with. A stale proposed change blocks the entire apply batch, verified with milestone/risk/budget-item staleness cases.
- [x] Apply transaction: exact literal `"APPLY"` confirmation (never a hidden Boolean); actor/role revalidated before any transaction opens; reload option + `APPROVED` decision + every `PENDING` proposed change (require ≥1); re-check staleness; apply each domain mutation (allowlisted fields only — milestone `currentDate`, risk's proposed field subset, budget item's proposed field subset; `NEW_ACTION` mutates no domain table); one shared `appliedAt` for every change in the batch; one `CHANGES_APPLIED` `AuditEvent` linked to the decision. No AI or network call inside the transaction (verified: `AI_MODE` stays `mock` throughout every test).
- [x] Idempotency and concurrency: a repeated apply finds zero `PENDING` proposed changes and is rejected with no duplicate mutation/audit/`appliedAt`; two concurrent applies produce exactly one successful transaction via a conditional `updateMany(... WHERE status = 'PENDING')` claim — verified directly, both for decisions and for applies.
- [x] Web: `/programs/edgelink-x/analyses/[id]/options/[optionId]/decision` (role-gated verdict controls; a structured proposed-change editor — add/remove sections per change type via typed client-side state, never a free-form JSON textarea; accessible labels, field-level errors) and `.../apply` (read-only preview for every role; per-change target/old-value/new-value/stale-warning display; explicit "nothing has been applied yet" statement; apply control for Program Manager only). Analysis workspace gained per-option decision status, actor, rationale, and links. `StatusBadge` extended with `PENDING`/`APPROVED`/`REJECTED`/`REVISION_REQUESTED`/`APPLIED`/`CONFLICT` tones.
- [x] **Real bug found and fixed via the Playwright test, not the smoke test:** `decision/actions.ts` and `apply/actions.ts` each exported a plain object constant (`initialDecisionFormState`/`initialApplyFormState`) alongside their `"use server"` async function, which Next.js rejects outright ("A `\"use server\"` file can only export async functions, found object") — invisible to every GET-only smoke-test check, since the failure only manifests when a bound action reference is actually resolved for a real submission. Fixed by moving both constants into their client components, matching the identical pattern `events/new/event-entry-form.tsx` already established for the same class of bug in Phase 3.
- [x] Integration tests: `schemas.test.ts` (34 pure Zod-contract tests — every proposed-change discriminator, the decision discriminated union, unknown-field/client-supplied-oldValue rejection), `record-decision.test.ts` (26 tests — validation, authorization for all 3 roles, full state-machine matrix, concurrent-decision race, server-generated-oldValue proof, cross-program/unknown-target rejection, transaction rollback, audit redaction), `apply-changes.test.ts` (20 tests — validation/preconditions, authorization, stale detection for all 3 target types plus a mixed-batch case, successful milestone/risk/budget/NEW_ACTION application, shared `appliedAt`, audit linkage, idempotency, concurrency) — 80 new tests (468 → 548 core), 15 web unchanged, 563 combined.
- [x] **Disabled Vitest cross-file parallelism** (`fileParallelism: false`) after discovering a genuine flaky race between this phase's new domain-mutating tests and pre-existing exact-aggregate assertions (`readiness-snapshot.test.ts`, `evidence.test.ts`) running concurrently against the same seeded program — reproduced directly (a different test failed on each of several consecutive runs before the fix). Also rewrote `apply-changes.test.ts` to use its own temporary `Component`/`Milestone`/`Risk`/`BudgetItem` fixtures rather than mutating shared seeded rows at all.
- [x] Playwright: `@playwright/test` added to `apps/web`; `playwright.config.ts` (loads `.env.test`, forces `AI_MODE=mock`, never `missionthread_dev`); `e2e/global-setup.ts` (resets/reseeds the test database via the existing `db:reset:test` command before the suite runs); `e2e/decision-workflow.spec.ts` — one happy-path test: sign in as Program Manager, open the seeded successful analysis, approve one pending mitigation option with a milestone-date proposed change through the real UI, confirm the apply preview, type the exact `APPLY` confirmation, apply, verify the option shows `APPROVED`, the proposed change shows `APPLIED`, the actual milestone date changed, and the audit page shows both `DECISION_RECORDED` and `CHANGES_APPLIED`. `npm run test:e2e` (root) builds then runs the suite. Deliberately not added to `.github/workflows/ci.yml` — Phase 8 owns full CI browser-test expansion.
- [x] Smoke test extended (60 → 82 checks): unauthenticated decision/apply routes redirect to `/login`; role-based decision-page controls (Program Manager sees Approve/Reject; Engineering Lead sees only Request-revision; Executive Viewer sees no mutation controls) against a controlled, cleaned-up-afterward temporary `MitigationOption` fixture (inserted via `pg`, not Prisma/tsx — this script stays a plain Node script); apply-preview rendering against a second controlled fixture (a temporary `APPROVED` option with one `PENDING` proposed change) — Program Manager sees the Apply control, Engineering Lead does not. Never calls `applyApprovedChanges()` — read-only rendering checks only, per the task's "without destructive application" instruction; the one live approve→apply flow is Playwright's job.
- [x] Seed unchanged by design: still one successful analysis, three `PENDING` mitigation options, zero decisions, zero proposed changes, zero applied changes after a fresh reset — verified directly after every `db:reset:test` this phase performed.
- [x] Docs: `docs/DECISIONS.md` (17 new "(Phase 5)" entries — the `Approval`→`Decision` merge reconciled against `SPEC.md` §11, the state machine and its database-enforced uniqueness, decision permissions, the decision input contract, the resolved `NEW_ACTION` target-field conflict, the `NEW_ACTION` MVP representation, server-generated snapshots, stale-data detection, the decision and apply transactions, idempotency/concurrency, the `CHANGES_APPLIED` audit payload, the Vitest parallelism fix, the `"use server"` object-export bug, and the Playwright test infrastructure), `docs/ARCHITECTURE.md` (new "Approval and apply workflow — implemented (Phase 5)" section, updated request/data-flow diagram, updated Auth/domain-model sections), `README.md`, this file.
- [x] Quality gate: fresh `AskUserQuestion` authorization obtained before every destructive database operation this phase performed (the schema migration's dev/test application, and each `db:reset:test` — including the ones Playwright's global setup triggers); `node -v`, `npm ci`, `db:validate`, `db:generate`, `db:reset:test`, `lint`, `format:check`, `typecheck` (all 3 workspaces), `test` (548 core + 15 web = 563), `build`, `smoke:test` (82/82), `test:e2e` (1/1) all passing. Confirmed: no test/CI step calls the live provider; CI still `AI_MODE=mock`; AI never approves, rejects, requests revision, or applies anything — every decision and apply call requires a real, revalidated human actor; each mitigation option receives at most one decision (database-enforced); rejected/revision-requested options have zero proposed changes; a successful apply is atomic and idempotent; a stale apply performs zero mutations; audit records remain append-only (no update/delete path added); no secret or untrusted-note text appears in any audit payload or rationale-adjacent field; Phase 5 created no Phase 6 (security/evals), React Flow, rate-limiting, or MCP functionality.

## Phase 6 — Security and evals (not started)

## Phase 7 — Graph and MCP (not started)

## Phase 8 — Delivery (not started)
