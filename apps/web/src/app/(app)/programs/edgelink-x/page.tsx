export default function ProgramOverviewPage() {
  return (
    <>
      <h1 className="text-xl font-semibold text-foreground">EdgeLink-X Program Overview</h1>
      <div className="mt-6 rounded-lg border border-border bg-surface p-6 text-sm text-muted">
        The full program overview (requirements, milestones, dependencies, event entry) is built in
        Phase 3. Phase 1 confirms this route is reachable, authenticated, and styled consistently
        with the rest of the shell.
      </div>
    </>
  );
}
