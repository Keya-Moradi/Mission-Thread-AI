export default function AuditPage() {
  return (
    <>
      <h1 className="text-xl font-semibold text-foreground">Audit History</h1>
      <div className="mt-6 rounded-lg border border-border bg-surface p-6 text-sm text-muted">
        The filterable, append-only audit history is built in Phase 5, once analyses, approvals, and
        applied changes exist to audit. Phase 1 confirms this route is reachable, authenticated, and
        styled consistently with the rest of the shell.
      </div>
    </>
  );
}
